<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
      <h1>Welcome <%= Helpers.current_user(session).username %></h1>
      <h3>Your Concert List <%= Helpers.current_user(session).shows %></h3>
      <a href='/logout'>Log Out</a>
    <% else %>
      You Must <a href="/">Log In</a> to View Your Concert List!
    <% end %>
  </body>
</html>